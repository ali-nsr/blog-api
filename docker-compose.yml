version: "3.9"

services:
  backend:
    container_name: backend
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./config:/app
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=django-insecure-ex9311w^qbb0(+i-sva89)d_!zxny1g(bk$6)zbp0nx^*$_es-
      - DEBUG=True
    